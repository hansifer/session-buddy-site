---
import '@fontsource/inter';
import '@fontsource/inter/500.css';
import '@fontsource/inter/600.css';
import '@fontsource/inter/700.css';
import '@fontsource/inter/800.css';
import '@fontsource/inter/900.css';

import { ClientRouter } from 'astro:transitions';
import { Navbar } from '@/components/Navbar';
import type { Mode } from '@/components/Navbar';
import { Footer } from '@/components/Footer';
import { Spacer } from '@/components/Spacer';

export interface Props {
  title: string;
  mode?: Mode;
}

const { title, mode = 'default' } = Astro.props;
---

<!doctype html>
<html
  lang="en"
  class="bg-primaryBackgroundColor"
>
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width"
    />
    <link
      rel="icon"
      href="/favicon.ico"
    />
    <meta
      name="generator"
      content={Astro.generator}
    />
    <meta
      name="description"
      content="Session Buddy is the complete solution for managing your browser tabs and bookmarks like a pro."
    />
    <title>{title}</title>
    <ClientRouter />
  </head>
  <body class="bg-primaryBackgroundColor">
    <div
      id="app"
      style={{ display: 'contents' }}
    >
      <Navbar
        mode={mode}
        client:load
        transition:persist
      />
      <Spacer />
      <div>
        <slot />
      </div>
      <Spacer />
      <Footer client:load />
    </div>
  </body>
</html>
