---
title: v4.1.0
description: October 26, 2025
slug: releases/4.1.0
sidebar:
  order: 993
interimVisible: true
---

## 🔗 &nbsp; Deep links

The URL and title of the Session Buddy tab now includes information about which collection is selected. This has the following benefits:

<ul>
  <li style="margin-top:32px;">A Session Buddy collection can be bookmarked</li>
  <li style="margin-top:4px;">Collection selection history can be navigated with the browser’s back/forth buttons</li>
  <li style="margin-top:4px;">Collection selection is preserved when a Session Buddy tab is reloaded</li>
</ul>

## ⚡ &nbsp; Automatic backups

Collections and events are now automatically backed up locally to help protect from [unexpected occurrences](/data-reliability/).

As part of this feature, the [Restore](/restore/) dialog has been updated to include the option to restore from an automatic backup in addition to a file.

## ✨ &nbsp; Enhancements

- An action bar now appears when tabs or links are selected, highlighting actions that apply to the selection
- It is now possible to move a selected link or folder within a collection using keyboard shortcuts `alt+↑` / `alt+↓` or `option+↑` / `option+↓`
- Collections can now be color-coded
- [Export](/import-export/) has a new option to copy to clipboard when exporting selected collections
- **Unify folders** now consolidates only selected items if folders or links are selected
- The collection header now displays a full date instead of a relative date. It now also shows the creation date in addition to the update date.
- **Close** and **Save & close** actions are no longer disabled for the current window
- **Copy as** and **Sort by** link actions have been tidied up to reduce menu clutter
- Collection and event caching has been adjusted to improve user experience when multiple Session Buddy tabs are in use

## 🛠️ &nbsp; Fixes

- Fixed a regression causing the OK button to no longer be the default action on confirmation dialogs
- Added a workaround for a known browser bug that occasionally prevents an extension from opening when its icon is clicked
- Added a workaround for a known browser bug that can sometimes suspend Session Buddy’s snapshot cycle, halting the creation of snapshots
- The content pane is now properly focused after sorting links or unifying folders
- Fixed an issue causing Session Buddy to sometimes open in another window when clicking the extension icon if the icon is not pinned
- Fixed a bug preventing certain formats from being correctly detected during Import when dropping a Session Buddy v3 export file or pasting text with a [BOM](https://en.wikipedia.org/wiki/Byte_order_mark)
- Fixed a bug causing an extra snapshot to sometimes be created on browser restart
